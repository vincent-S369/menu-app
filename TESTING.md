# 手动测试清单

每次修改代码后，请按照下面清单手动测试一遍，确认核心功能没有被改坏。

## 基础页面

- [ ] 页面能正常打开，没有空白页或明显报错。
- [ ] 五个入口能正常切换：菜谱库、每周菜单、采购清单、新增菜谱、数据备份。

## 菜谱库

- [ ] 菜谱能新增。
- [ ] 菜谱能编辑。
- [ ] 菜谱能删除。

## 每周菜单

- [ ] 每周菜单能新增。
- [ ] 每周菜单能编辑。
- [ ] 每周菜单能删除。
- [ ] 每周菜单按星期和餐次排序。
- [ ] 同一天同一餐不能重复添加。

## 菜谱和菜单联动

- [ ] 删除菜谱后，相关菜单会同步删除。
- [ ] 编辑菜谱名后，菜单中的菜名会同步更新。

## 采购清单

- [ ] 采购清单能根据每周菜单自动生成。
- [ ] 采购清单能合并重复食材。
- [ ] 采购清单能勾选已购买。

## 本地保存

- [ ] 刷新页面后，菜谱、每周菜单和采购清单勾选状态仍然保留。

## 数据备份

- [ ] 数据备份入口能正常打开。
- [ ] 数据备份页面显示说明文字：用于备份或迁移本地菜谱、每周菜单和采购清单状态。
- [ ] 点击“导出数据”后，可以下载 JSON 文件。
- [ ] 导出的 JSON 文件包含 `recipes`、`weeklyMenus` 和 `checkedItems`。
- [ ] 导出的 JSON 文件名包含当前日期，例如 `menu-app-backup-2026-xx-xx.json`。
- [ ] 点击“导入数据文件”后，可以选择 JSON 文件。
- [ ] 导入前会弹窗确认：“导入会覆盖当前本地数据，确定继续吗？”
- [ ] 导入确认弹窗中选择取消后，不会覆盖当前数据。
- [ ] 确认导入后，数据会写入 `localStorage`，页面上的数据统计会更新。
- [ ] 导入成功后，会提示“导入成功”。
- [ ] 导入后切换到菜谱库、每周菜单、采购清单，能看到导入的数据。
- [ ] 导入错误 JSON 文件或格式不正确的文件时，会提示“文件格式不正确”。
- [ ] 导入错误文件后，页面不会崩溃，原有数据不会被覆盖。
